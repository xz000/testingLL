6561|         let cx = sw / 2.0;
6562| 
6563|         // 标题区
6564|         let title = "术士之战 Warlock Brawl";
6565|         draw_text(&mut canvas, ctx, title, 54.0, graphics::Color::from_rgb(255, 210, 120), Point2 { x: cx, y: sh * 0.14 
6566|         draw_text(&mut canvas, ctx, "—— 选择对战模式 ——", 22.0, graphics::Color::from_rgb(200, 205, 215), Point2 { x: cx, y: s
6567| 
6568|         // 卡片通用尺寸
6569|         let card_w = (sw * 0.62).min(560.0);
6570|         let card_h = 96.0;
6571|         let card_x = cx - card_w / 2.0;
6572|         let y0 = sh * 0.34;
6573|         let gap = 26.0;
6574| 
6575|         #[cfg(feature = "steam")]
6576|         let in_lobby_menu = self.steam_lobby_menu || self.steam_lobby_create || self.steam_lobby_list;
6577|         #[cfg(not(feature = "steam"))]
6578|         let in_lobby_menu = false;
6579| 
6580|         // Steam 大厅：建房设置 / 房间列表 / 大厅主界面三种子界面。
6581|         if in_lobby_menu {
6582|             // 建房设置界面。
6583|             #[cfg(feature = "steam")]
6584|             if self.steam_lobby_create {
6585|                 self.draw_steam_create_lobby(&mut canvas, ctx)?;
6586|                 if self.room_cfg_edit {
6587|                     self.draw_room_cfg_editor(&mut canvas, ctx)?;
6588|                 }
6589|                 canvas.finish(ctx)?;
6590|                 return Ok(());
6591|             }
6592|             // 房间列表界面。
6593|             #[cfg(feature = "steam")]
6594|             if self.steam_lobby_list {
6595|                 self.draw_steam_lobby_list(&mut canvas, ctx)?;
6596|                 if self.room_cfg_edit {
6597|                     self.draw_room_cfg_editor(&mut canvas, ctx)?;
6598|                 }
6599|                 canvas.finish(ctx)?;
6600|                 return Ok(());