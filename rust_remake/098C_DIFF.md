# 098c 与 rust_remake 差异对比（2026-09-05）

> 来源：`098c/`（用户解包的 Warlock 098c 地图，含完整破解产物：war3map.j 337KB 反编译、
> MECHANICS.md 机制全记录、spells/items JSON）。逐项与 rust_remake 当前实现对比。
> **基准问题**：rust_remake 此前以 098b 知识库（`port_098b/`）为真值；098c 是其后续版本，
> 多处机制**本质不同**。A 类为基准冲突需拍板，B 类为 098c 新信息可回填（无争议），
> C 类为已确认一致项。
>
> 按用户指示：以 rust_remake 为基础吸收 098c 机制，**不按 GGEPSEUDO.md 伪代码重写**。

---

## A 类：基准冲突（需要拍板——098c 与 098b 机制本质不同）

### A1 · 魔法系统（最大差异，推翻 D3 的部分依据）

| | 098b（我们的依据） | 098c（解包实证） |
|---|---|---|
| 出生魔法 | 10000（恒满） | **0**（`fX`: 魔法=0, HP=满） |
| 魔法来源 | 无（无回蓝） | **挨打回魔**：`dX(目标,dmg)` 魔法 += 受到伤害 |
| 魔法用途 | 击退公式常数项 | 击退公式 **活变量**：`(100+魔法)` 从 100 → 最高 10100（**约 100 倍**） |
| 技能耗蓝 | 无 | **也无**（amcs 全 0；"消耗"=冷却+状态限制） |
| 定位 | 惰性池 | **核心张力**：魔法=资源兼"负重"，残血+满魔会被一击推飞 |

- 量化：HP 池 100、魔法池 10000 → 打掉 10 HP ≈ 目标获得 10 魔法（击退公式 +10/10100）。
- **我们的现状（2026-09-09 复核，已对齐）**：D3 删掉的只是 **098b 的「施法耗蓝」蓝量系统**
  （`mana_max/mana_regen/spend_mana/mana_cost` 一并移除）。但 **098c 风格「挨打回魔→放大击退」的活变量
  `Player.mana` 在 D9 已重新落地**，并非折叠成常量：
  - `player.rs:224` `pub mana: f64`——注释「放大所受击退（公式 100+mana），纯内部值，无 UI 蓝条」；
    出生置 0（`player.rs:794` `self.mana=0.0; // 098c 出生魔法 0（D9）`）。
  - 挨打回魔：`world.rs:1132` `p.mana += dealt`、`:2479` `p.mana += dmg`（对应 098c `dX`）。
  - 击退活变量：`world.rs:311` `warlock_ki_knockback(victim_mana,gx,ji)= (100+victim_mana)×gx×ji`，
    调用点 `:1916 / :2496 / :3233`；`balance.rs:91` 亦注明「击退初速=(100+目标当前魔法)×gX×JI（动态）」。
  - 回归测试 `mana_tension_gain_and_knockback_scaling`（world.rs:5775）验证：出生 0 → 火球直伤 7 后
    `mana=7`、击退初速 `(100+7)×7×1.1≈824/s`。
  - **结论**：A1 表格「魔法用途=击退公式活变量」这一点**我们已实现**，与 098c 一致；A1 此部分实质已对齐，
    无需再「恢复」。剩余仅 A4 的 `Gn[施法者]`（伤害成长）与 A7 的 `hn/Hn`（头盔击退减免）尚未并入击退公式。
- 附加：TimeShift 回溯时魔法重算 `(0.8-zr/10)×(gn-ee)+ee`（zr=怀表等级）。

> **2026-09-09 已核实（用户疑问：原版释放技能是否要魔法？—— 不要，A1 无误）**
> 解码 `war3map_pretty.j` 确认：**098c 释放任何技能都不消耗魔法**。
> - 技能耗蓝 `amcs` 在 w3a 对象数据里全 0（JASS 不引用 amcs）；施法分发 `SC=GetSpellAbilityId()`（line 8017）
>   走 `Ab/mC/jb/Pb/Ub/…` 各函数，**没有任何一个调用扣魔 `DX`**。
> - 扣魔函数 `DX`（line 919 `gn-=cX`）只被 **治疗/Drain/减速物品** 等具体效果调用（1419 治疗、7862 Drain、7652/7768/7870 怀表减速），与「释放技能」无关。
> - 魔法 `gn` **只增不减地来自挨打**：`dX(id,dmg)`（line 915）在受击结算 `hI`（3297）、伤害飘字（3485/3520）、岩浆（3589）处把 `gn+=受到伤害`；出生命 `fX`（927）置 0。
> - 魔法唯一用途是**击退公式活变量** `(100+gn)`（MECHANICS §4.1），即「残血+满魔会被一击推飞」的核心张力。
> - **结论**：用户的直觉正确——原版放技能零耗蓝。A1 表格「技能耗蓝=无（amcs 全 0）」与 JASS 一致，**A1 在「耗蓝」这一点没有问题**。
>   A1 真正要拍板的，只是「是否恢复 挨打回魔 + 魔法放大击退 这一套张力机制」（选项 1），**不是**给放技能加蓝耗。D3 删蓝量导致的偏差，也仅在于丢了击退随目标魔法放大的手感，而非「放不出技能」。

### A2 · 移动模型（文档原话"Warlock 的灵魂"）

| | 我们的实现（098b 口径） | 098c |
|---|---|---|
| 模型 | 加速度 1312.5 / 减速度 2625 直插满速 | **冲量滑行**：每次右键 `速度 += 18 × 0.5^(当前速度/20) × 方向` |
| 手感 | 匀速走位 | 低速加速猛、高速衰减 → 惯性滑行 |
| 帧积分 | `pos += vel×dt` | 同（0.03s/帧） |

### A3 · 物理碰撞

| | 我们的实现 | 098c |
|---|---|---|
| 碰撞检测 | 离散（帧内重叠后分离） | **扫掠连续**（t∈[0,1] 求交，防高速穿透） |
| 实体互碰 | 位置分离推挤 | **动量交换**（质量 dv=0.5，`(dv[a]+dv[b]+2)/4` 系数） |
| 撞墙 | 钳制+出界熔岩 | **反弹** `vel = -vel×0.5`（弹性 xv=0.5）+ 位置钳制 |

> **2026-09-09 已分析（结论：动量交换已完成，其余两项不建议照搬）**
>
> **动量交换 ✅ 已落地**：`resolve_player_collisions`（world.rs:4190-4198，注释标注「098c 动量交换 D9 批次2」），
> 沿碰撞法线交换速度分量、保留切向分量。
>
> **扫掠 CCD —— 收益低于预期，暂不做**：098c 需要它是因为**物理帧 0.03s（33Hz）**；
> 而我们跑 **1/60（60Hz）**，每帧位移更小。实测口径：最快弹体 900/s（S017）→ 每帧仅 **15 单位**，
> 而最小碰撞直径 = 弹体 23 + 柱子 24 = **47**（对玩家更大，约 53）。余量 3 倍以上，正常帧率下不会穿透。
> 仅在大 dt（卡顿/低帧率）或极小半径目标下有理论风险 ⇒ 属防御性加固，非 bug。
>
> **墙反弹 —— 在我们的架构下没有对应场景，不建议照搬**：098c 的「墙」是**地图硬边界 ±4096**
> （MECHANICS.md:75 `vel=-vel*0.5` + 位置钳制），而我们的 `arena_radius` 是**缩圈边界**，
> 圈外=岩浆，对应 098c 的「岩浆/可出界区域」（MECHANICS.md:77，RA 返回 6）—— 那一类区域 098c 也是
> **可以进、进去掉血**，并不反弹。两者语义本就一致。要照搬墙反弹需先引入「硬墙」这一新机制，
> 且会破坏现有的出界熔岩玩法 ⇒ 超出「对齐 098c」范畴，属产品设计决策。
>
> **2026-09-09 补：柱子（柱子）是否即「墙反弹」的对应物？—— 否。**
> 用户假设 A3 墙反弹可能对应我们场景里的**柱子（圆形障碍）**。经核对 098c：
> - 「墙」与「柱子」是**两个不同的对象**：墙=地图边界 ±4096（MECHANICS §10 `Kx/lx=-4096`）；
>   柱子=场内圆形障碍 r35（§8），半径/HP/可摧毁独立。
> - 098c 柱子**不反弹**：§8 明确「被击退冲量为 0（不动）、会被火球摧毁、导弹撞柱即碎（`IA` 销毁）」。
>   即柱子是**掩体/可破坏物**，弹体撞上碎掉，而非弹性反弹。
> - 我们已实现 098c 柱子行为：`resolve_obstacles`（玩家分离+截断强制位移）、`step_projectiles` 1b 段
>   （回旋镖镜向反弹、其余弹体被挡下、火球类扣柱子 HP40 至销毁，world.rs:1457-1507）、闪电 Bb 段反射
>   （world.rs:3181-3238）。其中**回旋镖/闪电反弹**是我们基于 098b 手感的增强（098c 原版所有弹体撞柱皆碎），
>   属手感取舍，非 bug，亦非 A3 的「墙反弹」。
> - 结论：A3 墙反弹 ≠ 柱子；柱子已正确实现 098c 掩体语义。A3 维持结案，无需为柱子加「墙反弹」。

### A4 · 伤害倍率成长（W 系翻盘机制，098c 独有）

> **2026-09-09 已解码核实（JASS 确认存在）**
> - 命中敌人 `Gn[施法者] ×= 1.1`（连乘）：`war3map_pretty.j:6383` `set Gn[Vv[Vr]]=Gn[Vv[Vr]]*1.1`。
> - `Gn` 其它改动（**非「成长变体」，澄清 2026-09-09**）：
>   - `5678`(nB,灼烧到期)`Gn×10` = 撤销灼烧开始时的 ×0.1，对应我们 `gn_factor` 的 `healing_blocked?0.1`，**已等价覆盖**。
>   - `2698`(XI)`Gn×0.5` = **每轮开局/重生**对所有玩家减半：`xf/Bf/Cf/If` 四个回合开始函数 + 重生 `2766` 均调用（**含默认 FFA** `xf`，非模式专属）；其解除 `EI`(×2,`2691`) 全 JASS 从不调用（死代码）。因每轮 `Gn` 重置 `1.0`(`10234`)，净效果 = **每轮开局 `Gn=0.5`**。
>   - 默认/重置 `Gn[i]=1.`（`9042` 作弊面板、`10234` 开局）。
> - 头顶伤害点数 `ai[施法者]`（MECHANICS §4.3）。
> - **我们的现状（2026-09-09 复核：已实现，文档原「真实缺口」为过时结论）**：伤害成长 **已在 D9 批次1 落地**，
>   `gn_factor()` 现已含 `growth`(`player.rs:509` `growth×tied×weakened×禁疗0.1`)，`on_dealt_damage`（`player.rs:523`）
>   命中后 `growth *= 1.1`，出生/重轮 `player.rs:795` 重置 1.0，`world.rs:1107` 实际伤害乘 `gn`；
>   爆炸命中非 owner 也 `world.rs:2505` 触发 `Gn×1.1`。测试 `world.rs:5795`「命中敌人应成长 Gn×1.1」通过。
>   **未对齐/未做（均极小，暂不跟）**：① 每轮开局 `Gn=0.5`(098c 经 XI) 我们为 1.0 —— 开局伤害高 2×，×1.1 成长几发即抹平；② 头顶 `ai[]` 点数 HUD 纯 UI 未做。核心 ×1.1 成长、灼烧 ×0.1、虚弱 ×0.5 均已覆盖。A4 实质已对齐。

### A5 · 计分

> **2026-09-09 已解码核实（修正文档误差）**
> 098c 计分是**独立三项**累加进 `Ln[]`（记分板），常量面板显示为 "Kill/Assist/Win Point Reward"
> （`war3map_pretty.j:9386-9390`）：变量 `ko`(击杀) / `Ko`(助攻) / `mo`(胜利)，值取自 globals
> 声明 `integer ko=1 / Ko=1 / mo=2`（line 2）。
> - MECHANICS.md §5「击杀→2 分」是**文档误差**（实际 `ko=1`）；「胜利→2 / 助攻→1」正确。
> - AI_DIFFICULTY_NORMAL 分支额外把 `ko=1, mo=0`（`10292/10294`），但 globals 默认即 **击杀1 / 助攻1 / 胜利2**。

| | 我们（098b ed()） | 098c（JASS 实际） |
|---|---|---|
| 轮胜利 | +1 | **+2**（`mo=2`） |
| 击杀 | +1 | +1（`ko=1`，**非文档写的 2**） |
| 助攻 | +1 | +1（`Ko=1`，按 Jn 伤害占比判定） |

> **结论（2026-09-09 复核 + 2026-09-09 修正代码）**：098c 实际 **击杀1/助攻1/胜利2**（globals `ko=1/Ko=1/mo=2`，
> `war3map_pretty.j:2`；计分累加 `:2903/+Ko`、`:2907/+ko`、`:2436/:2587/+mo`）。此前 MECHANICS.md「击杀2」是笔误，
> 且 D9 批次3 曾把笔误照抄进代码（`meta.rs` 旧 `score_per_kill:2`）。**已修正为 `score_per_kill:1`**
> （提交于本回合），现代码与 098c 三项完全一致：击杀1/助攻1/胜利2。A5 已结案，无待实现项。

### A6 · 生命回复

> **2026-09-09 已解码核实（确认无基础回血）+ 复核：已对齐，文档原 0.05 为旧值**
> - 098c JASS 中**没有任何每帧 `Fn[id]+=` 的被动回血**：唯一正向加血是 `BX`（治疗/吸血/技能，line 906），
>   岩浆 `nA` 只减不增；单位对象数据 `uhpr=0`（无基础回血）。即**回复全靠物品/技能**。
> - 我们的现状（复核）：`balance.rs:75` `hp_regen: 0.0`，`world.rs:577` 注释「回复全靠物品/技能」
>   （回血只来自 `item_fx.regen_add` 等）。**已与 098c 对齐**，文档早先写的 0.05 是改动前的旧值。A6 已结案，无待实现项。

### A7 · 尺寸/半径细差

| 项 | 098c | 我们 | 差 |
|---|---|---|---|
| 术士碰撞 | `do=30` | 32 | 微 |
| 柱子半径 | `fo=35` 固定 | 24–40 随机 | 微（随机波动是用户认可的改良） |
| 弹体半径 | 44 | 火 25/追 29/回 40/陨 72 | 大（098c 统一 44？待核 per-spell） |
| 竞技场 | 约 [-4096,4096]（8192 宽，墙由 wpm 定） | 半径 640（用户体感） | 尺度体系不同，不能直接换算 |
| 死亡阈值 | HP < 0.5 | HP ≤ 0 | 微 |

### A8 · 岩浆伤害（恒定，无回合成长）

> **2026-09-09 已解码核实（结论：098c 岩浆为恒定 9/s，无回合成长；我们此前线性占位系文档误差）**
>
> - 098c 实证（war3map_pretty.j）：岩浆每跳由定时器 `sa`（0.1s → `$A=10` 跳/秒）驱动 `nA`，
>   扣 `Fn[id]-=To[id]`。基础 `To[id]≈0.9`（开局 `ud` 设 `.9`、加 bot 设 `1.0`），
>   显示 `To[0]*$A` → **约 9/s 恒定**。
> - **全 JASS 无「随回合数成长」的缩放**：枚举所有 `To[...]` 赋值，仅国王模式对君主 ±10% 抗岩浆
>   （`To[kV]=To[kV]*9/10` 加冕 / `*10/9` 退位，line 12253/12398）、物品减速（`1/.96` 等）、
>   `-constants` 作弊设值、开局 `.9`/加 bot `1.0`。MECHANICS.md §5「To[0] 随回合数成长」为**文档误差**。
> - 「拖延越久越痛」的真实来源是**缩圈**（暴露面积增大），已由 `shrink_arena` 实现，与 098c 一致。
> - 我们：`out_hurt=9.0`（balance.rs:82）已与 098c 吻合；world.rs 原 `round_scale=round_number`
>   线性占位**已移除**（改为恒定 1.0）。A8 实质已对齐，**无待实现项**。

---

## B 类：098c 新信息 → 可回填我们的 TODO（无争议）

| 项 | 098c 信息 | 回填点 |
|---|---|---|
| 沉默系统 | 技能替换 Disabled 版；时长=5s+沉默等级-技能等级差；R 系减半；怀表缩短 | M3 2c 占位的实现规格 |
| 助攻判定 | `Jn[受者×12+施法者]` 伤害累计 → **按占比**发助攻 | 我们伤害矩阵已就位，补占比口径 |
| First Blood | 全场第一杀播报 | 连杀播报旁 |
| Draw 平局 | 平局加赛一轮文案 | round_over 平局分支 |
| Scourge 模式 | 效果码 985/954（击退/伤害 ×0.8/×0.4），雪人装饰 | En 模式候选 |
| 柱子可摧毁 | HP 40（浮动文字显示），火球类可毁，掉 Shard 重生 | 柱子系统增强 |
| TimeShift 细节 | 复制**全部物理状态**到影子对象；回溯重算魔法 | S006 增强 |
| 镜像 S022 | `Mirror` 技能（098b 疑废弃位，098c 实为 Mirror） | 名册订正 |
| 击退衰减参考 | `hr=Gr×0.04`；TimeShift `ov=0.97/帧` | D8 衰减模型旁证 |
| Toggle 变体全表 | S032-036 五键切换对应关系完整（Meteor↔Magma、Drain Slow/Weaken、Swap 两种、Gravity 两种、Rush 两种） | 形态系统（S032-36）规格 |

## C 类：已确认一致（无需动作）

- 技能无原生耗蓝（amcs 全 0）——D3"无蓝量消耗"正确（但魔法系统 A1 另论）；
- 热键布局 G/D/E/R/T/Y/C/F；槽位语义（G=普攻、Y=终极、C=防御、F=特殊）；
- 多数 CD（火球 4.8、闪电 16.5→12.5、瞬移 16→11.5、TimeShift 3.6s 等）；
- 物品家族/效果（靴/盔/斗篷/坠饰/面具 24% 吸血/熔岩靴/怀表/乔丹之石）；
- 伤害矩阵 → 助攻（我们已实现同构 Jn 思路）；
-乔丹之石（学终极/破上限）概念。

---

## 待拍板（A 类走向）

1. ~~**A1 魔法系统**~~ **✅ 击退活变量已落地（2026-09-09 复核）**：D9 已用 `Player.mana`（出生 0、
   `damage_player` 挨打回魔、`warlock_ki_knockback=(100+mana)×gx×ji` 动态化）实现 098c 核心张力，
   与 098c 一致；D3 仅移除 098b 的「施法耗蓝」资源（`mana_max/regen/spend/mana_cost`）。
   A1 此部分**无需再改**。仅剩：TimeShift 回溯重算魔法（`player.rs:219` 附近公式待补）、
   以及把 A4 的 `Gn[施法者]` 成长与 A7 的 `hn/Hn` 头盔击退减免并入击退公式（当前 `warlock_ki_knockback`
   仅含 victim_mana/gx/ji）。
2. **A2 移动模型**：切换冲量滑行（`18×0.5^(v/20)`）还是保持加速/减速？
   - 手感差异大；098c 文档称其为灵魂。建议切（且与击退衰减共用速度积分架构）。
3. ~~**A3 物理**~~ **✅ 已结案（2026-09-09）**：动量交换已落地；扫掠 CCD 因我们跑 60Hz（098c 是 33Hz）
   而收益低，暂不做；墙反弹在我们的架构下无对应场景（无硬墙），且会破坏出界熔岩玩法，不照搬。
   详见 §A3 的分析批注。
4. ~~**A4 伤害成长**~~ **✅ 已落地（2026-09-09 复核）**：`Player.growth` 连乘 ×1.1、出生/重轮重置 1.0、伤害链乘 `gn_factor()` 均已实现（D9 批次1），测试通过；仅 `×10`/`×0.5` 变体与头顶点数 HUD 未接。无待实现项。
5. ~~**A5 计分**~~ **✅ 已结案（2026-09-09 核实+改码）**：098c 实测 **击杀1/助攻1/胜利2**（JASS 实证）；代码旧 `score_per_kill:2` 系照抄 MECHANICS 笔误，已改为 1（`meta.rs`），现三项与 098c 完全一致。无待实现项。
6. ~~**A6 回血**~~ **✅ 已对齐（2026-09-09 复核）**：`balance.rs` `hp_regen:0.0`，回血全靠物品/技能，已与 098c 无被动回血一致；文档早先 0.05 为旧值。无待实现项。
7. ~~**A7/A8**~~ **A8 已结案（2026-09-09）**：098c 岩浆为**恒定 9/s**，无回合成长（MECHANICS.md「随回合数成长」为文档误差，已核对 JASS 全部 `To[...]` 赋值）；成长感来自缩圈。我们的 `round_scale` 线性占位已改为恒定，详见 §A8。`out_hurt=9.0` 已与 098c 吻合。A8 无待实现项。
