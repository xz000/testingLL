# 0.98b 复刻工程决策记录（权威）

> 创建 2026-09-04。本文件是「用 rust_remake 复刻 Warlock 0.98b」的**工程决策权威记录**，
> 与知识库 `port_098b/`（内容真值）互补：那里回答"0.98b 是什么样的"，这里回答"我们怎么建"。
>
> 阅读顺序：`port_098b/index.md`（知识库入口）→ 本文件（工程决策）→ `port_098b/00_总览/port_plan_098b.md`
> （原移植计划，**其技术选型以本文件为准**，冲突处一律按本文件）。
>
> 原 `PLAN.md`（Unity demo 重写计划）的目标已由本文件接替；历史文档保留作背景。

---

## 一、总路线：复用 rust_remake，不另起炉灶

**决定**：rust_remake 作为 0.98b 的引擎继续演进；`port_098b/` 作为内容真值库喂技能/物品/机制数值。

**理由**：本项目最难且与具体内容无关的部分已经完成并经审计加固（RISK_ANALYSIS.md 全部 P0/P1 项）：
定点数确定性帧同步（Fix64）、Steam P2P 联机（大厅/配置同步/重连/主机迁移）、确定性 Rng、
Buff/弹体原型（直射/追踪/回旋/弹跳/链式/撒弹/场）、障碍与射线、快照/重连。
0.98b 的内容（41 技能、24 物品、模式/商店/复活流程）以「换血 + 扩展」的方式落进现有架构，
成本远低于重写；重写等于丢掉唯一难以重来的部分（确定性 netcode）。

---

## 二、已拍板的技术决定

### D1 · 逻辑 TICK 保持 1/60（覆盖 port_plan 的 0.03）

- 098b 本体逻辑步长 0.03s（≈33.3Hz）；我们**不复刻它的步长**，保持现有 `TICK = 1/60`。
- 前提成立的原因：本工程所有速率类量都以「单位/秒」存储、每 tick 乘 `TICK`，步长只影响
  离散步进的粒度，不影响数值口径；帧同步只要求**全端同 TICK**，不要求与原版同 TICK。
- **关键纪律**：098b 伤害/公式里的 `.03`（如 `kI = ('d'+mana)*gX*...*.03`）是**几何换算常量**，
  与逻辑步长无关（port_plan §5）。移植时按**字面常量 0.03** 抄，**绝不写成 `TICK`** ——
  这是两套时间体系拼接时最容易静默出错的点。
- 收益：`HOST_DROP_TICKS`/`CLIENT_STALE_TICKS`/`RECONNECT_RESP_INTERVAL` 等按帧计的
  网络常量全部不用动。

### D2 · 单位尺度直接切 war3，不引入缩放系数

- 所有空间量（距离/半径/速度）直接用 war3 单位（`port_spec_098b.json` 的 speed 即单位/秒，直接填）。
- **否决全局缩放系数方案**：全量复刻下旧数值本来就不保留，系数是永久税——每移植一个数值都要
  手动 ×k，漏乘一处就是静默 bug，比一次性普查贵。
- 数值来源表（M0 落地值）：

| 量 | 值 | 来源 |
|---|---|---|
| 英雄移速 | 210 | `port_spec_098b.json` engine.hero_movespeed |
| 英雄 HP | 100 | w3u h000 术士（与旧值相同 → **伤害类数值不缩放**） |
| 英雄蓝上限 | ~~10000~~ → 蓝量系统整体移除 | 见 D3 |
| 英雄碰撞半径 | 16 | h000 未覆盖 Collision → 继承基础单位 hpea（农民=16）；**TODO(M5) 真机核对** |
| 场地初始半径 | 1200 | **占位**：知识库未给出场地尺寸（陨石射程 1200、闪现 700+ 佐证半径在 1000+ 量级）；**TODO(M5) 对齐 098b 场地** |
| 缩圈速度 | 21/s | 占位：保持旧比例 1.75%/s × 1200 |
| 出界/熔岩伤害 | 9/s | mechanics §五 熔岩 `Uo*10 = 0.9×10` |
| 加/减速度 | 1312.5 / 2625 | 占位：保持旧加速时间（0.16s/0.08s 到全速） |

- **Fix64 平方溢出警示**：Fix64 是 I32F32（上限≈2.1e9），全仓距离判定普遍用平方比较，
  平方安全上限 ≈46340。098b 真实数值（AoE 250–400、施法 1200）都安全；但
  **`aran=99999`（全图施法哨兵）平方必然溢出**（release 已开 overflow-checks → 直接 panic）。
  移植被此哨兵的技能时必须用「无限射程」分支表达，**哨兵值不得进入平方运算**。
- **版本纪律**：任何改变快照/配置字段或数值语义的提交，bump `CONFIG_VERSION`
  （`progress.rs`）并更新版本历史注释。

### D3 · 移除蓝量系统（实现其真实效果，不留蓝条）

0.98b 蓝量事实（依据 `jass_deobf.md` / `_spell_handlers.txt` / w3u 导出）：
施法不耗蓝（全技能 `aman` 为空）、无回蓝、唯一流动是 <0.05% 量级的特例扰动
（分裂弹每命中 −4、虔诚奶友 −0.5DX、一处 +10）→ **实战蓝恒满**，是个惰性池。

**落地**：
- 伤害公式的 `(100 + 蓝量) × 0.03` 折叠为具名常量 **`DAMAGE_BASE = 303.0`**（100+10000=10100 满蓝），
  写入 `balance.rs`，注释保留原式出处。
  **语义修正（M1，读码后确认）**：该项只出现在**击退速度**里——KI 公式
  `Q[hR] += (100+mana)×gX×Gn×hn×.03×Hn×JI×dir` 是击退速度累积；**实际伤害走 FI：
  `伤害 = gX × Gn[攻方] × hn[受击方]`**（不含蓝量、不含 0.03；火球 L1 gX=7.0 → 7 HP，
  对 100 HP 英雄 ~14 发，量级自洽）。098b 移植口径：
  - 伤害 = `gX(等级公式) × Gn(攻方放大，M1=1) × hn(受击方受伤系数，M1=1)`；
  - 击退初速 = `DAMAGE_BASE × gX × JI`（098b 为逐帧衰减模型，M1 先以恒速 push 近似，
    时长/封顶为手感标定值，TODO 对齐衰减模型 `每帧×0.96`）。
  注：KI 公式取的蓝量项是**受击者**的（`UnitState(F[hR],MANA)`，hR=受击方）——恒满时取谁的蓝都是常数。
- 删除：`Player.mana/max_mana/mana_regen`、`regen_mana`/`spend_mana`、施法蓝量门槛、
  `Attributes.mana_max/mana_regen` 与 `GrowthAttr::ManaMax/ManaRegen`、技能 `mana_cost` 字段、
  HUD/学习界面 U/I 购买键、快照与配置编码中的蓝量字段（CONFIG_VERSION 4→5）。
- 各技能交互按效果落地、不带蓝：
  - 分裂弹：保留命中计数与「帽子戏法/吸血鬼」播报，去掉 −4 蓝；
  - 虔诚：治疗 + 移速加成照做，去掉蓝转移；
  - 时光回溯：快照位置/生命/朝向，不含蓝；
  - 汲取虚弱形态：按 control 文档的 debuff 实现（其 `'d'/('d'+蓝)` 乘子与主公式蓝项
    恰好约掉，折叠为常数后自然退化）。
- **待定（不阻塞）**：`fv` 状态（某类无敌，「伤害按法力折算」）——移植到设置 fv 的技能时
  再决定按普通无敌处理还是保留小内部池；届时在此记录。

### D5 · 对局流程：不移植 098b 轮内复活，由现有「全员多轮」承载（2026-09-04 讨论定案）

- **事实核对**：098b 宏观循环 = 开局购物 40s → 战斗 → 每轮购物 30s → 再战（多轮）；
  轮内死亡 10±0.5s 复活仅是 war3 引擎下实现「这轮没打完别退出」的滚动手段。
  rust_remake 现有 = `Fighting → Learning → …`（`total_rounds` 轮），轮内打至
  `alive_count<=1`，轮尾 `reset_round()` **全员**满血+出生环+清场+换地形进下一轮，
  **无整场淘汰**——两边宏观结构（多轮/轮间购买/经济成长/累计胜负）一一对应。
- **决定**：复活/尸体/复活点/「死里逃生」彩蛋整块不移植。
- **已知偏差（接受）**：轮内死亡代价不同（098b 死一次损失 10s+随机复活点 vs 坐至轮尾）。
  若将来要原版节奏，「复活混战」可作为 En 模式之一补做（`alive` 状态机仍在，无需预埋）。

### D6 · 经济与得分：按 098b 工资制 + 分数体系（2026-09-04 讨论定案）

以 `ed()` 默认值为准（`-C` 17 项对应可调，见 mechanics §3.3）：

| 项 | 变量 | 098b 默认 | 落地 |
|---|---|---|---|
| 开局金 | So | 20 | `starting_gold` 默认 20 |
| 每轮金 | so | 10（人人） | `gold_per_round` 默认 10，轮结算点发放 |
| 击杀金/助攻金/胜利金/最高伤害金 | mo/Mo/Po/qo | **全 0** | 改配置项默认 0（纯工资制） |
| 击杀分/助攻分/胜利分 | lo/Lo/po | 1/1/1 | **新增 score 体系**，En1 按总分定胜负 |
| 名次奖励 | —（无此机制） | — | `place_rewards` 保留配置、**默认关闭**（自研玩法可开） |

- **拍板三点**：① 名次奖励默认关；② 「轮胜利」= 每轮最后存活者 +1 分（组队=胜队全员，
  随 M4 队伍系统）；③ 助攻判定 = 本局内对该死者有过伤害记录的其他玩家（无时间窗；
  098b 原版精确口径未解码，近似标定）。
- **新增基础设施**：`World` 确定性伤害矩阵（谁对谁多少伤害，随轮清空，进快照）——
  同时解锁助攻判定、最高伤害统计（`An[]` 对应）、`qo` 奖励三条线（098b 对应 `Jn[]/An[]`）。
- M4 范围修订：删除复活/尸体/复活点条目；保留 En 模式框架（先 En1 回合+En2 得分）、
  经济对齐、助攻统计、连杀播报（`Mn[3..10]`）、购物时长（开局 40s/每轮 30s）。

### D7 · Nn 生命恢复与陨石灼烧禁疗（2026-09-04 解码记录）

- `zd()` 每玩家 `Nn=0.05` = **全局生命恢复 0.05/s（每 20s 回 1 血）**，`-C#9`「生命恢复」
  （显示 ×100）；`Balance` 补 `hp_regen: 0.05`。×100/×10 为 war3 菜单显示缩放惯例。
- `nB`（=S008 陨石灼烧 impact，映射 `Ba→nB`）解码：「烤肉饼」debuff =
  ① 持续 AoE 伤害（参数随等级，半径/每跳数值仍需对账）② **`Gn×0.1` 输出惩罚 4s**
  ③ **`Nn=0` 禁疗 4s**（定时器 `aB` 到点恢复旧值）。落地：Buff 系统加 `Scorched` 型。
- 推论：**Gn/hn 不是摆设**——098b 用 Gn 做 debuff（灼烧砍输出）。KI 伤害公式的
  `×Gn[攻]`/`×hn[守]` 应从可被 debuff 修改的玩家状态读取（M1 框架位的真实用法），
  S008 的 ignite TODO 由此部分回填。

### D8 · 尺寸与击退运动学对账（2026-09-05，知识库核对）

**逐项对账表**（真值 = w3u 物体导出 + JASS 常量表）：

| 项 | 098b 真值 | 当前值 | 结论 |
|---|---|---|---|
| 英雄移速 | h000/h003 `MoveSpeedBase=210` | 210 | ✅ 精确 |
| 缩圈方式 | war3 地形限制只能整块消失 | **连续缩圈**（刻意改良，用户确认）；速度按比例口径 1.75%/s → 11.2/s | ✅ 定案 |
| 英雄 HP | h000 `HP=100` | 100 | ✅ 精确 |
| 转向 | h000 `TurnRate=0`（瞬时） | 瞬时 | ✅ |
| 英雄碰撞 | h000 未覆盖 → war3 原版 hpea 默认（约 32） | **32**（2026-09-05 定案：用户体感「地图直径 ≈ 20 个术士并排」交叉验证，与场地 640 自洽） | ✅ 落定 |
| 场地半径 | 旁证：复活 `±ua*64`、火球弹程 1000、闪现 700–1330、陨石 cast_range 1200（全图落点） | **640**（= 20 术士并排 × 碰撞直径 64 ÷ 2；交叉验证：火球 1.56 半径横穿、闪现 1.2 半径非全图、8 人密度合理。原占位 1200 偏大——当时误把陨石 cast_range 当有效射程） | ✅ 落定（2026-09-05） |
| 冲撞速度 | `Hr = $514×.03 = 1300/s` | 1300 | ✅ 精确 |
| 弹体速度/半径 | 火 1000/追 900/汲 700/陨 400/喷 800/跳 900；Mr=75、Dr=29、Fr=200、Lr=70 | 已用 | ✅ |
| 生命恢复/熔岩 | Nn=0.05 / Uo×10=9 | 0.05 / 9 | ✅ |
| 全图哨兵 99999 | cast_range | 不进平方运算 | ✅（D2 纪律） |
| 干扰值排除 | 移速 522 = `cast` 测试单位、Collision=1 = u000-u004 dummy | — | ✅ 已排除 |

**圈外 = 熔岩（2026-09-05 用户洞察，知识库证据闭环）**：098b 的熔岩是 war3 编辑器
预铺的静态地形（JASS 9 处 `GetTerrainType==sa[ka]` 全是技能判定，无 SetTerrainType 生成），
其几何即「可玩区域之外」——且熔岩伤害 `Uo×10=9/s` 与我们的出界掉血同值。**不做独立
lava_pools**：出界语义统一为熔岩（`LAVA_HURT` 常量）。-0.1 hp/s 惩罚照常（自然回血 clamp 0）。

**熔岩靴激活式（2026-09-05 用户拍板 A 方案，贴原版）**：熔岩上施放天罚/虔诚 → 持靴者
激活 `LavaShield` buff（87.5% 抵抗 3/4/5s 按档位）+ 25s CD（`Player.lava_boot_cd` 随快照）；
窗口外穿着吃全额熔岩伤 + 惩罚，无靴不可激活。`BuffKind::LavaShield` 编码 tag 6。
「死里逃生」表现（熔岩→安全处闪现且残血触发）待表现层批次。

**柱子尺寸（2026-09-05 用户确认）**：与玩家相近的小幅随机波动 24~40 war3 单位
（保留旧版相对波动设计 1.1~1.6 × 碰撞因子 16）；环布局/旋转/抖动/数量随机不变。

**技能名册全量核对（2026-09-05）**：22 个实装技能与 `abilities_098b.md` 官方名录
逐一比对，**无一错挂**（roster 对账测试锁定名称+实装状态）。13 个未实装编号建
显式占位条目（名称标「未实装」、Unimplemented、不进学习可选列表）：
S022/S023/S028/S029 w3a 无字段（疑废弃位）不建条目；S024 物品 / S025-027 法术槽 /
S030 怀表沉默 / S031 锁链附加 / S032-036 切换键——依赖形态系统与物品战斗化，
占位可见防「误认为漏做」。

**物品实现状态（2026-09-05 盘点）**：24 个全部有数据层+购买/升级链+商店 UI；
5 个武器改件战斗钩子已接（面具/法杖/剑×2/盾×2）；3 项待依赖：
熔岩靴抵抗（熔岩地形 M5）、怀表沉默缩短（触发面未解码）、面具「天罚加倍」分支（未解码）。

**击退运动学回填（有出处）**：`jr = Hr×(1-.96) = 1.56` —— 击退速度**每 0.03s ×0.96**
（此前 M2 TODO「衰减模型未解码」由此解码）。落地：
- `Control.decay` 字段 + `Player::push_knockback(vel)`：初速按有效击退减免缩放，
  每帧 ×0.9776（0.96^(5/9)，0.03s 步换算到 1/60 步长），速度 <5 自然停止（名义上限 3.5s）；
- 098b 名册击退（W098b 命中/Bolt/Nova/陨石爆炸）走衰减模型；Unity 版旧技能保持恒速
  push 过渡（名册替换完成时统一）；冲撞 Kick 命中击退暂恒速 TODO；
- `W098B_KB_TIME=0.35` 恒速近似与 `W098B_KB_MAX_SPEED=2000` 封顶注释同步更新。

### D9 · 基准转向 098c（2026-09-05 用户拍板）与实施计划

> 背景：用户解包 098c（`098c/`，完整 JASS 反编译），对比见 `098C_DIFF.md`。
> 拍板：**基准全面转向 098c**；魔法系统按用户口径落地——**内部张力值**（无 UI 蓄能条、
> 无上限管理），不是旧式"蓝量"；A2-A8 全按 098c；B 类无争议直接做；不按 GGEPSEUDO 伪代码重写。

**批次 1 · 魔法张力 + 动态击退 + 伤害成长（A1/A4，核心手感）**
- `Player.mana: f64`：出生 0、挨打回魔（`damage_player` 目标 mana += 实际伤害）、
  无上限不 clamp（用户明确不要上限）、随快照同步。
- 击退公式动态化：`warlock_ki_knockback(victim_mana, gx, ji)` = `(100+mana) × gx × ji`（单位/秒；
  098c 的 `×0.03` 为帧长换算，与我们的 /s 口径相消）。DAMAGE_BASE=303 折叠退役
  （那是 098b"蓝恒满 10000"口径）。保留初速封顶防极端。
- 伤害成长 `Player.growth`（098c Gn）：命中敌人 ×1.1 连乘、死亡/重轮重置 1.0、
  头顶点数（客户端渲染）；灼烧「烤肉饼」= 期间 gn_factor 临时 ×0.1（不永久改成长）。
- 击退位移会较现值缩小（mana=0 基数 100 vs 303）——相关测试位移断言重标定。

**批次 2 · 移动手感 + 物理（A2/A3）——移动部分已回退（2026-09-05 用户实测反馈）**
- ~~滑行~~：**已回退**。把 098c 的 ov=0.96 阻尼误用于普通地面导致"按 S 停不下来"。
  源码核实（war3map_pretty.j 3882 行）：0.96 阻尼是**全局每帧**乘子，但它配合的是
  **事件驱动冲量**模型——每次右键 order 给冲量 `18×0.5^(速度/20)`（6271 行），玩家靠
  连点右键维持速度；我们的输入是连续 move_target 电平，语义不同，直接套阻尼参数
  等于把"冰面/TimeShift 特定状态"的手感套上普通地面。
- **普通地面恢复受控立刹**（accel 1312.5/decel 2625，用户实测确认自然）；
- 098c 冲量模型单独立项：需先把客户端输入语义从"电平"改为"事件"（右键按下即冲量、
  停止输入即滑停），并把 0.96 阻尼 + 冰面（-ice，`Ka` 开关：冰面上滑行阻尼生效）
  一起实现——那才是完整的 098c 移动系统。
- **保留**：动量交换（玩家互撞等质量完全弹性，与地面无关）、碰撞半径 30。
- 动量交换：玩家互撞沿法线交换速度分量（098c 等质量 dv=0.5 → 系数 1，完全弹性）。
- 扫掠碰撞**跳过**（有计算依据：我方最大弹速 1300/s=21.7/帧 << 最小判定直径 54，
  无穿透风险；098c 需要它因其速度累积体系不同）。
- 撞墙反弹跳过（098c 的墙在地图外界 ±4096，竞技场圈内玩法不触及；出圈=熔岩不变）。
- 碰撞半径 32→30（098c `do=30`）；柱子随机 24-40 保留（35 在范围内，用户认可波动）。

**批次 3 · 经济/环境（A5/A6/A8 + B 类）**
- 计分改 **2/2/1**（胜/杀/助，098c）。
- 删基础回血（098c uhpr=0；斗篷/坠饰 regen_add 保留；灼烧禁疗/惩罚逻辑不变）。
- 岩浆随回合成长：`LAVA_HURT × round`（098c To[0] 成长率未解码，线性占位可调）。
- 柱子可摧毁：`Obstacle.hp=40`，弹体命中扣血、归零移除（每轮 re-layout 即重生）。
- First Blood 播报；Draw 平局（无轮胜分进下一轮）。

**批次 4 · 规格/名册收尾（B 类）**
- 沉默：Tied 已承载禁施法 + 怀表缩短已接；098c 细化规格（5s+等级差、R 系减半）记 TODO。
- 助攻占比口径（098c 按 Jn 占比）记 TODO；S022 占位订正为「Mirror（未实装）」；
  TimeShift 回溯魔法 ×0.8 系数。

**执行顺序**：1 → 2 → 3 → 4，每批门禁全绿+提交。A7 尺寸细差随批次 2 落地。

### D4 · 技能名册换血：098b 为准，Unity 版数值全部过渡处理

- `DefTable` 逐技能替换为 098b 数值（`port_spec_098b.json` + `abilities_consolidated_098b.md`），
  热键槽位对齐 G/F/D/E/R/T/Y/C/P；现有 Unity 版 30 技能仅作机制原型参考。
- **098b 条目走 `warlock098b_def` 直通**（不经 `legacy_scale_def` 缩放）；Unity 版旧技能走
  `raw_def → legacy_scale_def` 过渡链。名册替换完成后把旧链整体删除。
- **growth 口径（M1 踩坑记录）**：`SkillGrowth` 语义是 `base + delta×(L-1)`，而 098b 公式是
  `基数 + 斜率×L`（L 从 1 起）——**base 必须填 L1 值**（如火球 gX=6.3+0.7L → base=7.0/delta=0.7），
  对账测试按 L1 与 Lmax 两端断言。
- **过渡缩放**（M0 引入，**名册替换完成时整体删除**）：旧技能的空间数值按三类因子一次性放大，
  集中在 `skill.rs` 的 `legacy_scale_def()` 单点生效（不在 30 个 def 条目里逐个改字面量）：
  - 速度类 ×65.625（=210/3.2，含 push_power/kick 击退力）
  - 距离类 ×60（=1200/20，含 range/max_distance/length）
  - 半径类 ×16（=16/1，含 radius/width）
  - 时长（秒）与伤害**不缩放**（HP 尺度 100 不变）
- 技能移植对账：每技能按 port_plan §9 写数值单测（speed/radius/CD/duration 断言）。

---

## 三、里程碑（修订版，接现有工程）

| 里程碑 | 内容 | 状态 |
|---|---|---|
| **M0 尺度与系统切换** | 本文件决策落地：war3 尺度、蓝量移除、DAMAGE_BASE、过渡缩放、相机自适应、CONFIG_VERSION=5 | ✅ 完成（`d63480c`） |
| **M1 098b 名册脚手架** | `SkillId` 098b 段（S000/S003/S004，id 36+）；`Warlock098b` effect + `ProjectileKind::W098b`（Straight/Homing/Boomerang 三种运动学）；KI/FI 结算框架（FI 伤害=gx×Gn×hn，M1 Gn/hn=1；击退=`min(DAMAGE_BASE×gx×JI, 2000)`×0.35s 恒速近似，TODO 对齐逐帧衰减）；S000 命中点燃场（复用 Star，75 半径 2.5s）；热键槽复用现有 CastKey（集合本就=098b 的 G/F/D/E/R/T/Y/C）；`MAX_SKILL_SLOTS` 36→64；数值对账测试 4 个 + 行为测试 3 个 | ✅ 完成（`37e77ad`） |
| **M2 全技能（主力 22 个）** | 四批完成：批次A 投射物系 6（S002/S008/S009/S014/S015/S016，提交 `3048f34`：Bounce 弹跳×0.8/跳+每跳寿命重置+全场扫描下一目标、blast AoE 复用 explode_at、锥形连发、Bolt 即时射线）；批次B 位移/增益 7（S005/S006/S007/S010/S011/S012/S013，`844465e`：W098bUtility 统一变体、rewind 锚点闪回、war3 加法移速按基础 210 换算乘数）；批次C 场/线控制 3（S017/S018/S019，`c5b6a99`：W098bOnHit 命中副作用、ChainPull 跳过击退、原地漩涡）；批次D AoE 光环 3（S001/S020/S021，`1144e51`：W098bNova+explode_at exclude_owner、灾变三级递进 catastrophe_stage 随快照）。名册 22/41，热键 D/E/T/R/C/F/G 全挂 | ✅ 主力战斗技能完成 |
| **M2 剩余（19 个非战斗/形态类）** | S022/S023/S028/S029（未识别）、S024 物品、S025-027 法术槽、S030 怀表、S031 锁链附加、S032-036 形态切换——依赖 M3 物品/商店与升级标志系统，归入 M3 | 移至 M3 |
| **M2 全技能** | 41 技能逐个移植（投射物类复用管线；控制/位移/场走 handler）；S032–S036 双形态；升级标志分支（Fa/Ga/Ha/ha/ga） | 未开始 |
| **M3 物品 + 商店** | item.rs 数据层（24 物品/8 条升级链/效果聚合，数值取自 w3t 导出）；PlayerProfile.buy_item（同家族替换=升级链）；商店 UI（学习期数字键购买）；乔丹之石破上限（alev+jordan）；武器改件战斗钩子（面具吸血/火球法杖点燃改写/鲜血之剑/守护之盾）；怀表增减益时长乘除。开局购物 Wo=40s 待 En 模式批接入 | ✅ 核心完成（`7e3da78`/`14f685e`；沉默 W000-007 触发面未解码，S017 以 Tied 承载记 TODO；零价档定价占位 TODO(shop)） |
| **M4 经济与结算（D6 第一批）** | World 伤害矩阵（助攻/最高伤害数据源，随快照同步、轮清空）；score 体系（击杀/助攻/轮胜 +1）；连杀播报（Mn[3..10]）；经济默认值对齐 098b（So=20/每轮 so=10/击杀金 0/名次奖默认空） | ✅ 完成（`cd2030f`；En 模式框架与 Wo=40s 开局购物 UI 为下一批） |
| **M4 流程改造** | ~~死亡→尸体→复活~~（D5：不移植）；击杀/助攻结算、连杀播报、En1/En2 模式 | ✅ 完成（`cd2030f`/`063202a`）；开局购物 Wo=40 阶段化与 En3-5 待做 |
| **M4 En 模式批** | MatchConfig.game_mode（En1 回合=默认 / En2 死亡竞赛）+ win_score（po）；En2 达分提前终局；final_ranking（分数降序、最优名次次序）+ 终局界面得分排名展示 | ✅ 完成（`063202a`） |
| **M4 开局购物 Wo=40** | `begin_first_round_config` 倒计时切到 `shopping_time_secs=40`（Wo），与每轮 `learn_time_secs=30`（wo）区分；商店/技能/属性购买在首局购物期全部可用（复用 Learning 期交互） | ✅ 完成（En3-5、队伍系统待后续批次） |
| **M5 场地与打磨** | 场地形状（圆/方/环/自定义）、柱子布局、熔岩带 9/s（熔岩靴抵抗接入）；英雄碰撞半径、场地尺寸两处占位在此里程碑用真机对账替换（复活点 BV[] 随 D5 取消） | 未开始 |

> 每个里程碑收尾：`check.ps1` 全绿（含 steam feature）+ 数值对账单测 + 本文件更新状态。

---

## 三之二、M2 数值缺口与简化备忘（M3+ 回补，均已在代码注释标 TODO）

| 项 | 状态 | 处理 |
|---|---|---|
| S009 分裂弹 speed（GB 未解码） | 近似 900 | 回查 jass_deobf |
| S014 汲取 yO/吸血/减速形态 | gX 近似 6+0.5L、双段合并 JI=0.8 | 随 S032 切换形态设计 |
| S008 陨石灼烧量（nB 数值未解码） | 未做（时长 4s 已知） | durations 对账 |
| S020 灾变 $B/$C/$E | 占位 12/16/20 | 解码后对齐 |
| S021 虔诚 DX / 队伍奶友 | 占位 8 / 只奶自己 | 随队伍系统（En 模式） |
| S017 致残 ri>0 AoE / MI 公式 | 未做 / 恒 3 占位 | 随属性系统 |
| S019 锁链 speed / S031 附加 / 链光 | 800 占位 / 未做 | 视觉+手感对齐 |
| S012 冲撞三段复合伤害 / 0.5s 定身 | 简化 5+0.4L / 未做 | bA 逐段对账 |
| S010 疾风步破隐一击 | 未做 | 复用 Kick 窗口 |
| S013 换位弹体化（speed 800/radius 40） | 即时换位近似 | 弹体管线 |
| S018 引力飞行段 / 拉速 | 原地漩涡 / 300 占位 | 弹体管线 |
| KI 击退衰减模型（每帧 ×~0.96） | 恒速 0.35s+封顶 2000 近似 | 手感标定 |

---

## 四、移植时的不变纪律

1. **确定性**：新增任何随机（复活抖动/掉落/商店）一律走 `World` 内确定性 Rng，禁止外部随机源。
2. **`.03` 按字面移植**，不绑 TICK（D1）。
3. **99999 哨兵不进平方运算**（D2）。
4. **改快照/配置结构必 bump CONFIG_VERSION**（D2）。
5. **数值疑议回知识库溯源**：spec → consolidated → jass_deobf → w3u 原始导出（见 `port_098b/index.md`）。

---

## 三之三、D11：098c 商店/物品/天罚全量解码（2026-09-05，war3map_pretty.j 反编译实证）

> 来源：`098c/out/war3map_pretty.j`（商店函数 bD ~L10590、天罚 mC L7599、守护盾 HC L7538、
> 熔岩靴 kC L7555、分派表 L8030-8160、国王/化身 L12235/L11950）+ `w3t_strings.txt` tooltip。
> **截图包《术士之战技能截图/》经核对非 098c**（电弧/冲击/爆炎/操纵/精通/研究均不在 098c 名册），
> 用户拍板不采纳，移植数值一律以下列 JASS 实证为准。

### D11.1 商店机制（bD）
- 物品购买 = 训练隐藏单位（h00B 靴/h002 斗篷/h00A 坠饰/h005 盔/h006 权杖/h001 面具/h004 乔丹/
  h007 血剑/h008 盾/h00C 熔岩靴/h00E 怀表）；`ID(id,AD,…)` 失败时**原额退款 AD** → AD 即买价。
- **每步同价**（升级=再训练一次，无阶梯价）：靴 5、斗篷 4、坠饰 5、盔 9、权杖 7、面具 12、
  乔丹 5、血剑 8、盾 13、熔岩靴 7、怀表 7。
- **gR/hR 不是金币**：gv[g]/0.03 = 每秒移速。升级链的 gR 调用全是移速修正——
  靴 +20/+10/+10、盔 -5/-5/-5、熔岩靴 +15/+12/+12（=27/39 与 tooltip 吻合）。
- `In[id]` = 回复（×10 = hp/s）：斗篷 +0.02/+0.01/+0.01（0.2/0.3/0.4）、坠饰3 +0.01、
  面具 -0.03、熔岩靴 -0.01。`jn[id]` = 时长标量（怀表 ×1.15/×1.25 实证）。
- `vi[id]` = 吸血指标 8%/点（L3299：PX(HI, HX×0.08×vi)）：面具 +3 = 24%；天罚期间 ×2 = 48%
  （mC `vi=2*vi`，结束 ÷2）。vi 另加成汲取时长（L8288）。

### D11.2 天罚 S001 = 普通局 F 键（分派表 L8053：S001→mC）
- **术士单位原生技能**（无需学习）；kn[7*i+7]='S021' 是学习表残留，运行时只授予 S001。
  早前「F=S021」结论（e5cbaf5）系误读，已修正为 F=[S001]。
- mC：伤害 cX = 10 + Zr（Zr=血剑数 +1/+2）；半径 250；衰减 1-d/1000；FX 自伤（0.5 保底）；
  命中 n 敌 → DX 回血 (Zr+1)×n；n≥3 弹「Hattrick」、戴面具弹「Vampire」。
- **面具**：天罚下吸血/回血翻倍；**熔岩靴**：站岩浆放天罚 → kC 触发 87.5% 抵抗窗口。
- S020 = **化身模式（En4）替换技能**（qC）；S021 = **国王山丘模式替换技能**（QC：
  伤敌同天罚 + 500 内队友 +60 移速 4s + 回血 cX/2；队友判定 cn[]，待队伍系统）。
- 国王：皇冠 + S001↔S021 + hn/To ×0.9；化身：S001↔S020 + 体型 1.4 + Rv=50 + Gn×1.5 + jn×1.2。

### D11.3 守护之盾 = 充能窗口（HC/jX/GX），非常驻减免
- 火球命中敌人 → 充能（Ha=true，item charges 灯 1）；
- 有充能时放天罚 → HC：5×jn 秒内 hn÷1.33（受伤 -25%；Aegis2 tooltip 75%）、Hn÷2（击退 -50%），
  消耗充能（jX：Ha=false、灯 0）。窗口在 FX 自伤之后挂。
- I00I（Aegis 2）**无购买分支**（098c 疑似残留），暂保留为升级链第二档。
- 岩浆 To[]：默认 0.9（岩浆伤 9/s，-C 1 可调）；kC 窗口 To÷8；熔岩靴常驻微减免 ×0.96/0.94/0.92。

### D11.4 形态切换 sC（L7899，S032-36 实锤）
Da[] 旗标切 handler：Splitter Target/Area、Meteor Meteorite/Magma、WindWalk Charge/Invisibility、
FireSpray Stream/Cluster、Drain Slow/Weaken、Gravity DarkMatter/ForceField、Link Hook/Induction。
S024-027 = 英雄形态切换单位（u000-u005）；S022 见 D12.4 修正（瞬移二段形态）；乔丹 = 解锁 T001-T006
「超上限 +2 级」按钮（iV[312]）。

### D11.5 本次代码落地（与本决策同步提交）
- item.rs：全目录对齐（斗篷 0.2/0.3/0.4 无移速惩罚、血剑 +1/+2 与回血 2/3、熔岩靴2=27、
  权杖 cost7/sell6、面具 cost12+翻倍、盾 13+充能语义、全部买价=训练价）。
- world.rs：smite_bonus 改平加；天罚回血（odh+吸血）×命中数×面具倍率；Aegis buff（5s）+
  充能（火球命中指纹 = Straight+Ki+点燃+无爆炸）；守护盾减免改窗口制。
- skill.rs：F 槽 = [S001]；S020/S021 注释改为化身/国王专属；S021 伤害 8→10。

---

## 三之四、D12：098c 游戏模式 / 队伍 / 精通系统解码（2026-09-05，二批考证）

> 触发：用户指出「098c 应该有精通系统」「有队伍和国王模式吗」——JASS 复查证实三者皆存在。
> 早前「精通属其他版本」「S022 废弃」结论均系依据 data/spells.json（仅覆盖技能本体）误判，特此修正。

### D12.1 五种游戏模式（聊天命令选择，nn 变量 L9173-9210）
| 命令 | 模式 | 098c 行为 |
|---|---|---|
| `-round`/`-rd` | ROUND（默认） | 常规轮次制 |
| `-deathmatch`/`-dm` | DEATHMATCH | 死斗（098b En2 同名） |
| `-avatar`/`-av` | AVATAR 化身 | 随机化身者：S001↔S020、体型1.4、Rv=50、Gn×1.5、jn×1.2、In 加成（L11950） |
| `-king`/`-kg` | **KING 国王** | 双王加皇冠+光环、S001↔S021、hn/To ×0.9（L12191 Cf） |
| `-lms` | LAST MAN STANDING | 最后生还 |
- 另有：`-ice` 冰面开关、`-circle/-square/±donut` 出生形状、`-xmas` 彩蛋、`-mgl` 固定 5v5（10 轮）。

### D12.2 队伍系统（cn[] = 队伍号）
- `-teams` host 对话框分配（Fd/nd）；`-teams 0121…` 按串直配；`-teamshuffle` 打乱。
- 敌我判定 `cn[a]==cn[b]`：S021 虔诚奶友/加速、团队分数 Ln[]、天罚 "Denied" 反杀文案。
- 默认 `cn[i]=i`（各自一队）；`-mgl` 前 5 人 0 队后 5 人 1 队。

### D12.3 精通系统 = 学习期研究科技（L12906，用户直觉正确 ✓）
研究 → 每购买一次对应战斗变量 +1（价格/上限在 w3q，解析失败待占位）：
| 科技 | 精通 | 变量 | 每级效果（JASS 实证） |
|---|---|---|---|
| R00D | 生命精通 | vi+1 | 吸血 +8%/级（`HX×0.08×vi`）；汲取时长加成（`3+yr+0.5vi`/`6+1.5yr+vi`） |
| R00I | 远程精通 | xi+1 | 火球爆炸范围 +12%/级（`pe×(1+0.12xi)`）；爆炸视觉 ×(1+0.2√xi)；冲撞附火球爆发 |
| R00Y | 时间精通 | ei+1 | 弹体寿命 ×(1+0.1ei)；闪电射程 600×(1+0.15ei)；冲撞距离 ×(1+0.1ei) |
- 共通：lf=vi+ei+xi，击退减免 +2.5%/级（Hn 复合公式）；**戴死亡面具 lf-3**（惩罚）。
- 另有 R000（火球等级研究，ur+1 → S000 等级）、R002（背包容量，S128 +1 级）。

### D12.4 S022 修正：瞬移「二段闪」形态（非废弃、非镜像）
学习 S011 时一并授予 S022 但默认禁用（L12933-12935），经 R 键切换键 S034 启用——
即截图 R1「瞬间移动 第二模式：二段闪（350+50x 两次短距闪现）」。早前「Mirror/废弃」说法作废；
名字 Mirror 系 098b 推测残留。R 槽形态旗标 = Fa[]（S012/S013 handler 亦按 Fa 分流）。

### D12.5 对我们复刻的影响（待办更新）
1. 精通三件套需实装（数值已实证；价格占位待定 w3q 或对照同类）。
2. 模式系统：先做 -round 默认 + king/avatar（S020/S021 已有近似实现），dm/lms 随后。
3. 队伍系统：cn[] 语义简单（整型队号），S021 奶友、团队分数依赖它。
4. S032-36 形态切换的实装优先级提升（S022 二段闪依赖它）。

---

## 三之五、D13：war3 局限 → 原生实现对照表（2026-09-06 定则）

> **原则（用户定则）**：098c 大量设计是 war3 引擎局限下的变通，我们只复刻**功能**，
> 落到原生 Steam 游戏上用更自然的载体；不再被 war3 的做法束缚。

| # | 098c 的 war3 变通 | 功能本质 | 我们的原生实现 | 状态 |
|---|---|---|---|---|
| 1 | 模式选择靠聊天命令（-round/-dm/-avatar/-king/-lms） | 选游戏模式 | **房间属性**：lobby/pre-game 房主设置项（模式/轮数/经济参数可视化配置；-C 17 项同入房间设置，聊天命令可留作高级快捷） | B3 |
| 2 | 物品购买=训练隐藏单位+失败原额退款 | 商店购买 | 商店 UI 直接扣钱（早已如此）；退款额只是「买价」的载体，不再有训练环节 | ✅ |
| 3 | 精通=研究科技（RESEARCH 事件+jn/In 等全局数组） | 学习期可购成长 | 学习面板直接购买（B1）；「不涨价/永久保留」语义保留 | B1 |
| 4 | 背包研究 R000→S128 物品能力等级 | 物品栏扩容 | profile.inventory_slots 字段（6→8），容量检查在 buy_item | B1 |
| 5 | 火球等级=R002 研究 IncUnitAbilityLevel(S000) | 火球可升级 | 与现有 G 槽「=」升级入口合并（同一件事），上限按 098c=10 | B1 |
| 6 | 乔丹解锁 T001-T006 隐藏升级按钮 | 技能可超上限 +2 | 直接并入升级上限判定（max_level+乔丹） | ✅ |
| 7 | 形态切换=S032-36 能力按钮 + 20 级数据 ±10 切换 | 每技能 A/B 两套形态 | **配置期免费切换**（用户定则）：学习菜单里 A/B 形态自由换，无对局内切换 | B4 |
| 8 | 冰面/岩浆=地形类型+0.1s 采样器 | 特殊地面 | 几何区域（已有）；**补**：站冰面回血 BX(In)、冰面不被岩浆侵蚀 | B5 |
| 9 | 缩圈=地形环 ta 逐环烧掉（每 10×√存活 秒一环 128 码） | 场地逐渐被岩浆吞没 | ✅ B5：半径 (9+n/2)×128 按人数、每 10√存活 秒烧一环（shrink_timer 步进）；全场吞没 2 人≈141s | ✅ |
| 10 | 昼夜钟 SetTimeOfDayScale 当缩圈进度条 | 缩圈倒计时可视化 | 直接画缩圈进度（环数/时间） | B6 |
| 11 | 多板记分板 | 计分显示 | 原生 HUD 记分板（B6 一并）；队分按 cn[] 聚合 | B2/B6 |
| 12 | 国王皇冠特效/化身体型 1.4 | 身份标识 | 状态圆环视觉（B6：金环=王、放大圈=化身等） | B3/B6 |
| 13 | 沉默=SetPlayerAbilityAvailable(false) 禁按钮 | 禁用技能槽 | 技能槽禁用状态（HUD 灰化+不可施法） | B4-Y |
| 14 | -stats/-apm 命令查询 | 信息查询 | HUD 常显或 Tab 面板 | B6 |
| 15 | -bot 加 AI（随机名生成器） | 补位人机 | AI 机器人（占位：B7 后可选） | B7+ |
| 16 | 死亡尸体单位+漂字 | 死亡反馈 | 粒子/淡出圆环+文字（B6） | B6 |
| 17 | 化身/国王、洗队依赖开局对话框+队伍色 | 团队玩法 | 房间属性设队伍；玩家色即队伍色（B2/B3） | B2/B3 |
| 18 | S021 虔诚奶友判定 cn[] | 同队判定 | Player.team 整型队号（098c 语义原样保留，这是纯逻辑不受引擎局限） | B2 |
| 19 | 20 级数据表驱动两形态数值 | 双形态数值 | SkillDef 双形态表（A/B 两套 growth），配置期选择即查表切换 | B4 |
| 20 | 复活=4s 计时器+随机点（DM）/凶手死救活受害者（LMS） | 复活规则 | world 内确定性复活调度（模式逻辑，与 war3 无关） | B3 |

> 持续增补：后续考证中每发现一个 war3 变通，对照本表归位；已在原生侧自然等价的直接标 ✅。
