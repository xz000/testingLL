# 技能/单位状态对齐审计（098c ↔ 我们）

> 起因：`Burnout`（需 `Hr` 燃烧冲刺）与 `Denied`（需 `Fv` 链索绑定）两个播报接不上，
> 怀疑「技能建模没完全对齐」。本文用工具扫出 098c 的**全部按单位/玩家索引的全局数组**并分类，
> 结论是：**真正缺的很少**，绝大多数数组是 War3「傀儡单位」管线的实现细节，我们已用原生模型替代。
>
> 工具：`tools/analyze_098c_state.py`（只读）：
> - 数组模式：`python tools/analyze_098c_state.py Hr Fv nv`（引用数 + set/read 样例）
> - 函数模式（**大小写敏感**）：`python tools/analyze_098c_state.py @AB @WB @mC`（函数体写了哪些状态 + 调了谁）
> 真值：`../098c/out/war3map_pretty.j`。

---

## 0. 结论（TL;DR）

098c 的按单位/玩家数组可分三类：

1. **War3 傀儡单位管线（实现细节，我们已用原生 `Projectile` 替代，不欠）**：
   `Av`（技能可用性 `id*3+n`）、`hv/jv/Gv`（boolexpr 条件/动作）、`Rv`（傀儡状态 id）、`ev`（傀儡寿命）、
   `Xv/Ev/cv`、`Q/S/U/w/Y/z`（傀儡速度/坐标增量）、`F`（傀儡单位）、`H`（闪电线）。
   → 098c 每次施法都 `CreateUnit` 一个傀儡承载法术；我们用 `Projectile`/区域/即时效果直接表达。**这不是缺口**。
2. **按玩家记账（基本已对齐）**：`Nn/bn`（在场/存活）、`cn/Vv`（队伍/归属）、`Rn`（本轮伤害）、
   `dn/Cn`（连杀计数/时刻）、`Fn`（当前 HP）、`Gn/Hn`（伤害/击退倍率）、`vi/ei/xi/oi/Zr`（等级/成长）。
3. **真·玩法副状态（少量，才是缺口）**：
   - `Hr`（**S012 A 燃烧冲刺**）→ Burnout；**我们未建**。
   - `Fr/gr`（**S010 A**）/ `fr`（**S010 B**）：带 debuff `gR` 的标记状态；**待核**我们 S010 是否等价。
   - `kr`（**S012 B 凤凰**瞬时状态）：我们有凤凰实现，**大概率等价**。
   - `Fv` + `Dv/fv/Nv/bv/Bv` + `Mb`：**通用「链接/绑定」系统**（S003/S005/S013/S017B/S019 等都用），
     我们按弹体实现，缺少「单位级绑定」及其交互（Denied）。**结构不同**。

→ **不是「很多技能没对齐」，而是 3 处技能的副状态**：S012 燃烧、S010 标记、通用链索绑定。

---

## 1. 分发表（skill → 实现函数，逐一核对）

from `war3map_pretty.j` 施法分发（约 16340–16620）：

| 技能 | A 形态（false） | B 形态（true） | 形态标志 |
|---|---|---|---|
| S000 | `Ab` | | |
| S001 | `mC`（含 Denied/Hattrick/Vampire） | | |
| S002 | `jb` | | |
| S003 | `Pb` | | |
| S004 | `Ub` | | |
| S005 | `gC` | | |
| S006 | `GC` | | |
| S007 | `KR` | | |
| S008 | `iB` | `OB` | `Da` |
| S009 | `GB` | | |
| S010 | `RB` | `IB` | `Da` |
| S011 | `HB`（含 Last Second Save） | | |
| S012 | `AB`（燃烧冲刺 `Hr`） | `WB`（凤凰 `kr`） | `Fa` |
| S013 | `MB` | `pB` | `Fa` |
| S014 | `ac` | | |
| S015 | `Ac` | | |
| S016 | `Gc` | `Dc` | `fa` |
| S017 | `rC` | `CC` | `ga` |
| S018 | `Jc` | `Mc` | `ga` |
| S019 | `uc` | `Uc` | `ga` |
| S020 | `qC` | | |
| S021 | `QC` | | |

> 注意大小写：`MB`(S013 A) ≠ `Mb`(链索绑定条件)，`AB`(S012 A) ≠ `Ab`(S000)。
> 工具的函数模式已按**大小写敏感**匹配。

---

## 2. 真·副状态明细（缺口清单）

### 2.1 `Hr` — S012 A 燃烧冲刺（**缺失**）
- `AB`(S012 A, 11916)：`Hr[ii]=true`、速度 `Q/S/U/w`、寿命 `ev`。
- 交互 `lb`(10961)：`Hr` 单位与**同队**单位 ≤45 → 对队友造成 `Xr=.45√(7+xi[team])`、写「Burn out」、清 `Hr`、速度 ×0.2。
- 我们：`Player.dash_active/dash_vel` 只有位移/击退/反弹，**无燃烧与队友熄火**。

### 2.2 `Fr/gr`（S010 A）/ `fr`（S010 B）（**待核**）
- `IB`(S010 B, 11890)：`gr[ii]=true; Fr[ii]=true; gR(ii, hR(ii)+Dr/2)`（加 debuff）。
- `RB`(S010 A, 11866)：`fr[ii]=true; gR(...)`。
- 读用：`7926 if Fr[nr] and (not Fr[Vr]) and gr[nr]`；`fr` 在 4895/7881 等。
- 我们：S010 = Break Strike / Windwalk / Charge（见 `skill.rs`）；需核对是否覆盖这些「标记 + debuff」交互。

### 2.3 `kr` — S012 B 凤凰（**大概率等价**）
- `WB`(S012 B, 12852)：`kr[ri]=true` + 触发器（`UB` 处理点令）。
- 我们：`Player.forms[S012]` + `s012b_phoenix_redirect_*` 已实现凤凰转向/发弹；`kr` 只是它的瞬时标志。

### 2.4 `Fv` + 链接族 — 通用「绑定/链接」系统（**结构不同**）
- 写 `Fv` 的技能实现：`Pb`(S003)、`gC`(S005)、`MB`/`pB`(S013)、`CC`(S017B)、`uc/Uc`(S019)、`Mb`（条件）。
- `Mb`(11162)：把 `nr` 绑到**最近敌人** `Fv[nr]=zO`；`aR`(4711) 解绑。
- 「连接规则」谓词：`Dv/fv`（5250 `return nv[gX]==1 or Dv[gX] or fv[gX]`）、`Av/Bv/Nv/bv`（`Mb` 条件 8409–8410）。
- 「Denied」：`mC/qC/QC`（S001/S020/S021 结算）中 `Fv[gX]==ii` 且（岩浆/`kr`/`Fr`）→ `aR(gX)` 断链。
- 我们：用 `ProjectileKind::Chain{last_target}` / Tether 弹体；**无单位级绑定**，故 Denied 交互缺失。

### 2.5 其余（`Mr/dr/ha/Nv/…`）
- `ha`：`Ub`(S004 回旋镖 11529/11533)、`Ac`(S015 13535) —— 疑似「弹体在空/返回中」。
- `Mr/dr`：`yc`/`VB`(S015 相关) —— 标记/联动。
- 这些多为**傀儡生命周期状态**（分类 1），我们以弹体状态等价表达，**默认不欠**；如需逐项核实可再挖。

---

## 3. 「傀儡管线」清单（**不欠**，仅记录）
`Av`（`id*3+n` 技能可用性）、`hv/jv/Gv`（boolexpr）、`Rv`（傀儡状态 id）、`ev`（寿命）、`Xv/Ev/cv`、
`Q/S/U/w/Y/z`（速度）、`F`（傀儡单位）、`G/H/J`（特效/闪电/texttag）、`le/te`（计时器）。
→ 098c 用「每次施法生成一个傀儡单位 + 状态机」实现弹体/区域；我们用 `Projectile` 原生实现。
**这是有意的架构差异，不是对齐缺口。**

---

## 4. 建议跟进（已收窄）
1. **S012 A 燃烧冲刺 + Burnout**（证据最全，范围可控）→ 补 `Hr` 与队友熄火；解锁 Burnout 播报。
2. **S010 A/B 标记**（`Fr/gr/fr`）→ 先核对我们的 S010 是否已覆盖 debuff/标记，再决定是否补。
3. **链接/绑定系统**（`Fv` 族）→ 决定「移植单位级绑定」还是「明确宣告弹体式为有意简化」；
   若移植，同时解锁 Denied 与若干链索交互。
4. **S012 B 凤凰**：抽查即可（大概率已等价）。

> 纪律：每项先写「098c 证据（行号）+ 现状 + 期望行为 + 单测」，再动代码。

---

## 5. 记录
- 2026-09-13：初版（确认 `Hr`/`Fv` 两缺口）。
- 2026-09-13：**第二轮**——扫描全集并分类，结论大幅收窄：绝大多数数组是 War3 傀儡管线（我们已用原生模型替代）；
  真·缺口只有 **S012 燃烧、S010 标记、通用链索绑定**三处（外加凤凰抽查）。补分发表与逐项证据。
